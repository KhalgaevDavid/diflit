1. Явный метод Эйлера
2. Метод Эйлера с пересчетом
3. Метод Коши
4. Семейство явных методов Рунге-Кутты второго порядка


Пусть решение определено на отрезке $[x_0,x_0+X]$, который разбит на $N$ равных частей с шагом $h=\frac{X}{N}$. узлы $x_i=x_0+ih,i=0,\ldots,N$. Приближенные значения решения в узлах будем обозначать $y_i$, в отличие от точных значений решения в узлах $y(x_i)$. 